[build]
  command = "pnpm build"
  functions = "netlify/functions"
  publish = "build"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["express", "serverless-http", "cors", "morgan"]

[[redirects]]
  from = "/api/*"
  to = ".netlify/functions/:splat"
  status = 200